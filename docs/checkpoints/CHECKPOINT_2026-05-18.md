# MADCORE Gena Checkpoint 2026-05-18

## Проект

- проект: `MADCORE Gena`
- локальный путь: `/home/max/MADCORE RF`
- плановый remote: `git@github.com:MaxCorpOrg/MADCORE-Gena-website.git`
- плановый production URL: `https://gena.madcore-kavkaz.ru`

## Что сделано

- на базе текущего `MADCORE-website` создана самостоятельная копия проекта;
- отделены:
  - бренд
  - контакты
  - CTA
  - tracking keys
  - deployment defaults
  - рекламные `cmp`
- добавлен канал `MaX` в UI и lead-flow;
- подготовлен серебряный визуальный слой;
- обновлены основные входные документы нового проекта.

## Что подтверждено

- исходный сайт в `/home/max/MADCORE` не изменялся кодовыми правками в этой папке;
- новый проект использует отдельные ключи `madcore_gena_*`;
- server defaults указывают на `/opt/madcore-gena`, а не на старую директорию.

## Что не завершено

- не создан GitHub remote;
- не создан отдельный бот;
- не созданы отдельные live идентификаторы аналитики;
- не выполнялась финальная выкладка на сервер;
- не выполнены итоговые `lint` и `build` после всех изменений.

## Что делать дальше

1. Дочистить оставшиеся project-wide ссылки на исходный сайт.
2. Создать новый GitHub-репозиторий и инициализировать git.
3. Создать отдельные внешние интеграции.
4. Прогнать проверки.
5. Подготовить server deployment.
